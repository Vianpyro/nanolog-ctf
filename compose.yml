services:
  nanolog:
    build: .
    restart: unless-stopped
    ports:
      - "1337:1337"
    pid: host   # Container escape vulnerability
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
